/** Larghezza layout template PDF (allineata a desktop/mobile). */
export const PDF_TEMPLATE_WIDTH = 800;

/** Altezza foglio A4 alla larghezza sopra (800 / 210 * 297). */
export const PDF_PAGE_HEIGHT = 1123;
